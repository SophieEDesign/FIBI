Base surface container.

```jsx
<Card tone="surface" elevation="sm" interactive>…</Card>
```

`night` uses the indigo lockup gradient for hero/empty moments; `brand` is the soft sky→orchid→gold wash. Never stack two elevated cards.
