Renders the FIBI logo from `assets/`. Set `window.FIBI_ASSETS` to change the base path.

```jsx
<Logo variant="mark" height={28} />
```

`mark` for app chrome, `full` for marketing headers and splash, `dark`/`light` are the original square lockups.
