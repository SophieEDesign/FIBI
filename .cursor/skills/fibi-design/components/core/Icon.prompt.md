Renders a Lucide icon. The host page must load `https://unpkg.com/lucide@latest/dist/umd/lucide.js`.

```jsx
<Icon name="map-pin" size={18} />
```

Default stroke is 1.75 — FIBI's icons are light, never filled.
