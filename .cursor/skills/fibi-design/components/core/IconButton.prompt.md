Circular icon-only button. Always give it a `label`.

```jsx
<IconButton icon={<Icon name="bookmark" />} label="Save" variant="glass" />
```

Use `glass` over imagery and maps, `surface` on plain backgrounds, `ghost` inside toolbars.
